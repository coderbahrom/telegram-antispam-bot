services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    # /addword orqali qo'shilgan so'zlar konteyner qayta ishga tushganda yo'qolmasligi uchun:
    volumes:
      - ./data/custom_keywords.json:/app/data/custom_keywords.json
